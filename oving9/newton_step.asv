function [p_k] = newton_step(hessian, gradient, x_k)
%NEWTON_STEP Summary of this function goes here
%   Detailed explanation goes here
subs(hessian, x1, x_k(1), x2, x_k(2));
p_k = -hessian(x_k)\gradient(k;
subs(p_k, x1, x_k(1), x2, x_k(2))
ret = sym2cell(p_k);
end

